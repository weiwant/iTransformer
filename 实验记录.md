# 实验记录 — iTransformer 复现

> 本项目**唯一的实验进度台账**，持续更新。最新结果以此为准。
> 项目指导、环境、命令、参数表、已知坑见 `CLAUDE.md`。
> 早期复现（2026-05-15，RTX 2060）原始记录：`D:\wqWork\论文\开题报告\周报\0520\T4_iTransformer复现\`。

---

## 1. 复现目标

复现论文 **Table 1 主表**：7 个数据集 × 4 个预测长度（pred_len = 96 / 192 / 336 / 720），多变量预测（features=M, seq_len=96），指标 **MSE / MAE**。

数据集：ETTh1、ETTh2、ETTm1、ETTm2、Weather、Electricity(ECL)、Traffic。

---

## 2. 实验环境

| 项目 | 开发机 | 服务器 |
|---|---|---|
| GPU | RTX 2060（6GB） | 2× RTX 4090（每卡 24GB） |
| 角色 | 写代码/脚本、修 bug、ETTh1 等小数据集冒烟测试、git 推送 | 跑全 7×4 表实验 |
| Python | 3.10（conda env `itransformer`） | 待搭建（建议同 3.10） |
| PyTorch | 2.0.0+cu118 | 待搭建（按 CUDA 选 cu118/cu121） |
| 状态 | ✅ 就绪 | ⏳ 待搭环境 + 下载数据 |

---

## 3. 进度总览（7 × 4，均按官方默认参数"严格复现"）

格内为复现 **MSE / MAE**。符号：✅ 复现成功（偏差 <8%）｜⚠️ 偏差大（>13%，待调参）｜📋 待跑（服务器）｜❌ 未开始

| 数据集 | pred_len 96 | 192 | 336 | 720 |
|---|---|---|---|---|
| **ETTh1** | ✅ 0.394 / 0.409 | ✅ 0.441 / 0.436 | ⚠️ 0.491 / 0.462 | ⚠️ 0.507 / 0.492 |
| **ETTh2** | ❌ | ❌ | ❌ | ❌ |
| **ETTm1** | ❌ | ❌ | ❌ | ❌ |
| **ETTm2** | ❌ | ❌ | ❌ | ❌ |
| **Weather** | ✅ 0.176 / 0.215 | 📋 | 📋 | 📋 |
| **Electricity(ECL)** | ✅ 0.148 / 0.240 | 📋 | 📋 | 📋 |
| **Traffic** | ✅ 0.393 / 0.269 | 📋 | 📋 | 📋 |

**完成度**：7 / 28 项（25%）。其中 ETTh1 长预测 2 项偏差大，需进入第二阶段调参。

---

## 4. 已完成结果（严格默认参数，迁移自 2026-05-15 记录）

### 4.1 ETTh1（全部 4 个 pred_len）

| pred_len | 复现 MSE | 复现 MAE | 训练轮数 | 最佳 Epoch |
|---|---|---|---|---|
| 96 | 0.3941 | 0.4086 | 7（早停） | 4 |
| 192 | 0.4413 | 0.4362 | 10（跑满） | 10 |
| 336 | 0.4912 | 0.4623 | 4（早停） | 1 |
| 720 | 0.5071 | 0.4923 | 4（早停） | 1 |

### 4.2 Weather / ECL / Traffic（各 pred_len=96）

| 数据集 | 复现 MSE | 复现 MAE | 训练轮数 | 最佳 Epoch |
|---|---|---|---|---|
| Weather 96 | 0.1760 | 0.2149 | 7（早停） | 4 |
| ECL 96 | 0.1479 | 0.2396 | 10（跑满） | 10 |
| Traffic 96 | 0.3930 | 0.2690 | 8+（终端中断，用 `--is_training 0` 续测） | 8 |

### 4.3 与论文 Table 1 对比（已确认项）

| 数据集 / pred_len | 论文 MSE | 论文 MAE | 复现 MSE | 复现 MAE | MSE 偏差 | 判定 |
|---|---|---|---|---|---|---|
| ETTh1 / 96 | 0.386 | 0.405 | 0.3941 | 0.4086 | +2.1% | ✅ |
| ETTh1 / 192 | 0.451 | 0.445 | 0.4413 | 0.4362 | −2.2% | ✅ |
| ETTh1 / 336 | 0.432 | 0.433 | 0.4912 | 0.4623 | +13.7% | ⚠️ |
| ETTh1 / 720 | 0.431 | 0.435 | 0.5071 | 0.4923 | +17.7% | ⚠️ |
| Weather / 96 | 0.164 | 0.210 | 0.1760 | 0.2149 | +7.3% | ✅ |
| ECL / 96 | 0.140 | 0.237 | 0.1479 | 0.2396 | +5.6% | ✅ |
| Traffic / 96 | 0.395 | 0.268 | 0.3930 | 0.2690 | −0.5% | ✅ |

> 短预测（96/192）偏差 −2.2%~+7.3%，复现成功。ETTh1 长预测（336/720）偏差 >13%，进入第二阶段调参。
> 其余数据集/pred_len 的论文参考值待服务器跑完后对照论文 Table 1 补全。

---

## 5. 当前阶段

**开发机侧项目初始化（2026-06-24）**：
- ✅ 生成 `CLAUDE.md`（项目指导）、`实验记录.md`（本台账）。
- ✅ 修改 `.gitignore`，忽略 dataset/checkpoints/results/test_results 等大文件。
- ✅ GitHub：`origin` 已指向 `git@github.com:weiwant/iTransformer.git`（SSH），文档与 .gitignore 已提交并推送到 `reproduce` 分支。
- ⏳ 服务器环境搭建 + 数据集下载（尚未开始）。
- ⏳ 全表实验（尚未在服务器开始）。

---

## 6. 待办计划

| # | 任务 | 负责方 | 备注 |
|---|---|---|---|
| 1 | ~~提供 GitHub 地址 → push~~ ✅ 已完成（origin=weiwant/iTransformer，`reproduce` 分支） | 开发机 | |
| 2 | 服务器搭环境（conda + torch cu118/cu121 + requirements）+ 下载数据集 | 服务器 | |
| 3 | 冒烟测试：ETTh1 pred_len=96 跑通 1~2 epoch 验证环境 | 服务器/开发机 | |
| 4 | **第一阶段**：严格默认参数跑全 7×4 表 | 服务器 | 填本文件 §3/§4 |
| 5 | **第二阶段**：对长预测偏差项（ETTh1 336/720 及其他 >5% 项）调参（train_epochs↑ / lradj=cosine） | 服务器 | 在 §7 记录调参版结果 |
| 6 | 汇总：补全论文对比表、输出最终复现结论 | 开发机 | |

---

## 7. 第二阶段调参结果（待填）

> 对偏差大的项做调参实验，记录于此。格式：数据集/pred_len | 调参内容 | 复现 MSE/MAE | vs 论文偏差 | 备注

（暂无）

---

## 8. 问题与解决

| 问题 | 现象 / 原因 | 解决 / 计划 |
|---|---|---|
| **CPU 版 torch** | `requirements.txt` 默认装 CPU 版，跑不了 GPU | 手动 `pip install torch==2.0.0 --index-url .../cu118` |
| **ETTh1 长预测偏差 >13%** | pred_len=336/720 最佳 epoch 出现在第 1 轮，疑似 `train_epochs=10` + `lradj=type1`（每 epoch lr 减半）欠拟合 | 第二阶段：增大 train_epochs、换 cosine lr 调度 |
| **Traffic 训练中断** | pred_len=96 训练至 epoch 8 终端被关闭 | checkpoint 已存最佳模型，用 `--is_training 0` 加载续测得到结果 |
| **setting 目录名错位** | 目录名前缀与参数错位拼接 | 照训练脚本原样填参数；仅测试时参数须与训练逐字一致（详见 CLAUDE.md §8） |
| **torch 覆盖 / mkl 冲突** | 旧 `pip install -r requirements.txt` 含 `torch==2.0.0`，覆盖 GPU torch，`import torch` 报 `undefined symbol: iJIT_NotifyEvent` | 已从 requirements.txt 移除 torch 行；cu118 版单独装 |

---

## 9. 变更日志

- **2026-06-24**：初始化本台账；迁移 0520 参照记录的 7 项已完成结果（ETTh1×4、Weather/ECL/Traffic×96，均严格默认参数）；建立项目文档框架（CLAUDE.md / 实验记录.md / .gitignore）。下一步：服务器 clone `reproduce` 分支 → 搭环境 → 跑全表。
- **2026-06-24（续）**：`origin` 改为 `git@github.com:weiwant/iTransformer.git`（SSH）；新建并推送 `reproduce` 分支（CLAUDE.md / 实验记录.md / .gitignore 已上传）。
- **2026-06-24（续2）**：新增 `scripts/reproduce/smoke_test.sh` 冒烟测试脚本（ETTh1 1 epoch 全流程 + `--is_training 0` 续测自检）+ `.gitattributes`（`*.sh` 强制 LF 行尾）。开发机自验通过后推送，待服务器冒烟。
- **2026-06-24（续3）**：修复 `requirements.txt`（移除 `torch==2.0.0`，避免覆盖 cu118 GPU torch 导致 `iJIT_NotifyEvent`）；CLAUDE.md 同步安装说明。服务器环境重建中。
- **2026-05-15**：（早期，RTX 2060）完成上述 7 项实验，记录于 `D:\wqWork\论文\开题报告\周报\0520\T4_iTransformer复现\`。
