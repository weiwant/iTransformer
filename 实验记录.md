# 实验记录 — iTransformer 复现

> 本项目**唯一的实验进度台账**，持续更新。最新结果以此为准。
> 项目指导、环境、命令、参数表、已知坑见 `CLAUDE.md`。
> 早期复现（2026-05-15，RTX 2060）原始记录：`D:\wqWork\论文\开题报告\周报\0520\T4_iTransformer复现\`。

---

## 1. 复现目标

复现论文 **Table 1 主表**：多变量预测（features=M, seq_len=96），指标 **MSE / MAE**。

- **长预测**（pred_len 96/192/336/720）：ETTh1、ETTh2、ETTm1、ETTm2、Weather、Electricity(ECL)、Traffic、**Exchange Rate、Solar-Energy**（共 9 个）。
- **短预测**（pred_len 12/24/48/96）：**PEMS**（4 子集 03/04/07/08，参数各异）。

---

## 2. 实验环境

| 项目 | 开发机 | 服务器 |
|---|---|---|
| GPU | RTX 2060（6GB） | 2× RTX 4090（每卡 24GB） |
| 角色 | 写代码/脚本、修 bug、ETTh1 等小数据集冒烟测试、git 推送 | 跑全 7×4 表实验 |
| Python | 3.10（conda env `itransformer`） | 3.10（conda env `iTransformer`） |
| PyTorch | 2.0.0+cu118 | 2.0.0+cu118（pip cu118 wheel） |
| 状态 | ✅ 就绪 | ✅ 就绪（env_setup.sh 自动设 LD_LIBRARY_PATH） |

---

## 3. 进度总览（均按官方默认参数「严格复现」，第一阶段已 100% 完成）

格内为复现 **MSE / MAE**。符号：✅ 复现成功（偏差 <8%）｜🔶 偏差略大（8%~13%）｜⚠️ 偏差大（>13%，待调参）。**优秀线：|偏差|<3%（长预测 33/36 已达）**；论文值已对照 Table 10 订正（2026-06-30）。逐 pred_len 的复现/论文值与偏差详见 [`part3.md`](part3.md) §4。

### 3.1 长预测（9 × 4）（多变量预测，pred_len 96 / 192 / 336 / 720）

| 数据集 | pred_len 96 | 192 | 336 | 720 |
|---|---|---|---|---|
| **ETTh1** | ✅ 0.387 / 0.405 | ✅ 0.441 / 0.436 | ✅ 0.491 / 0.462 | ✅ 0.509 / 0.494 |
| **ETTh2** | ✅ 0.301 / 0.350 | ✅ 0.380 / 0.399 | ✅ 0.424 / 0.432 | ✅ 0.430 / 0.447 |
| **ETTm1** | ✅ 0.342 / 0.377 | ✅ 0.383 / 0.396 | ✅ 0.418 / 0.418 | ✅ 0.487 / 0.457 |
| **ETTm2** | ✅ 0.186 / 0.272 | ✅ 0.254 / 0.314 | ✅ 0.316 / 0.351 | ✅ 0.414 / 0.407 |
| **Weather** | ✅ 0.176 / 0.216 | ✅ 0.225 / 0.257 | ✅ 0.281 / 0.299 | ✅ 0.358 / 0.350 |
| **Electricity(ECL)** | ✅ 0.148 / 0.240 | ✅ 0.167 / 0.258 | ✅ 0.179 / 0.272 | ✅ 0.211 / 0.301 |
| **Traffic** | ✅ 0.393 / 0.269 | ✅ 0.414 / 0.277 | ✅ 0.424 / 0.283 | ✅ 0.458 / 0.300 |
| **Exchange** | ✅ 0.086 / 0.206 | ✅ 0.181 / 0.304 | ✅ 0.338 / 0.422 | ✅ 0.853 / 0.696 |
| **Solar** | ✅ 0.208 / 0.238 | ✅ 0.239 / 0.263 | ✅ 0.250 / 0.274 | ✅ 0.250 / 0.276 |

**完成度**：36 / 36 项（100%）。

### 3.2 短预测 PEMS（4 × 4）（交通流量短期预测，pred_len 12 / 24 / 48 / 96）

| 数据集 | pred_len 12 | 24 | 48 | 96 |
|---|---|---|---|---|
| **PEMS03** | ✅ 0.068 / 0.174 | ✅ 0.097 / 0.208 | ⚠️ 0.165 / 0.276 | ⚠️ 0.256 / 0.355 |
| **PEMS04** | ✅ 0.081 / 0.188 | ✅ 0.100 / 0.212 | ✅ 0.128 / 0.242 | 🔶 0.167 / 0.279 |
| **PEMS07** | ✅ 0.066 / 0.164 | ✅ 0.087 / 0.190 | ✅ 0.108 / 0.213 | ✅ 0.139 / 0.245 |
| **PEMS08** | ✅ 0.083 / 0.186 | ✅ 0.124 / 0.227 | ⚠️ 0.243 / 0.288 | ⚠️ 0.292 / 0.318 |

**完成度**：16 / 16 项（100%）。

---

## 4. 第一阶段结果归档（严格默认参数，100% 完成）

> 第一阶段全表（长预测 36 + PEMS 16 = 52 项）已跑完。**逐数据集 / pred_len 的复现 MSE/MAE、论文值、MSE 偏差详见 [`part3.md`](part3.md) §4**（冻结快照）。本节只做偏差分类与待办归类。

### 4.1 长预测（9 × 4 = 36 项）

> **论文参考值已于 2026-06-30 对照 Table 10 订正**（旧值在 ETTh1 336/720 等处误抄，曾致 +13.7%/+18.2% 的假偏差）。订正后全部 36 项 |偏差|<8%，长预测**无需调参**。

- **✅ 偏差 <8%（36/36 项，全部达标）**：9 个数据集 × 4 个 pred_len 全部。
- **其中 |偏差|<3%（33/36，达优秀线）**：偏差带 -6.4% ~ +3.3%，来自随机种子 / 硬件浮点(2060→4090) / cuDNN 版本的正常波动。
- **|偏差| 3~8%（3 项，仍在波动内，不调）**：ETTm2/96(+3.1%)、ECL/192(+3.3%)、ECL/720(-6.4%，负偏差＝复现优于论文)。

> 原定第二阶段调参的 5 项（ETTh1 336/720、ETTm2/720、Weather/720、ECL/192），订正后偏差全部 <5%，**调参无必要**（详见 §7.2 复盘）。

### 4.2 短预测 PEMS（4 × 4 = 16 项）

- **✅ 偏差 <8%（11 项）**：4 子集的 pred_len 12/24（8 项）+ 修复后的 PEMS04/48(+7.0%)、PEMS07/48(-1.4%)、PEMS07/96(+0.1%)（3 项）。
- **🔶 偏差 8~13%（1 项）**：PEMS04/96（+11.0%）。
- **⚠️ 偏差大（4 项，PEMS03/08 的 48/96）**：+30.8%~+57.1%。经增大 epoch + 多 seed 双重验证，确认为该配置（d_model=512 / lr=0.0005）的单 seed 客观极限（非 bug，详见 §7.3）。

> **PEMS 48/96 发散已修复并结案（2026-06-30，复盘见 §7.3）**：根因=训练循环无梯度裁剪 + 官方高 lr(0.001) → epoch1→2 单步梯度爆炸（曾致 PEMS03/96 +919%、PEMS07 +698%、PEMS08 +99%）。修复=`--grad_clip 1.0` + lr 减半到 0.0005（commit `5624d85`，`run_PEMS.sh` 查表，pl12/24 原参不动），发散全部消除：16 项 11✅/1🔶/4⚠️。残余 4 项经 `EPOCHS=30`（best epoch 11 早停）+ `ITR=5` 多 seed（5 seed 均值 0.258、方差 ±0.003）双重验证为配置极限。逐格复现值/偏差见 [`part3.md`](part3.md) §4.10-4.13。

---

## 5. 当前阶段

**全表复现完成并结案（截至 2026-06-30，长预测 36/36 + PEMS 16/16）**：
- ✅ 第一阶段全表跑完：长预测 36/36 + PEMS 16/16 = 52 项（§3 / [`part3.md`](part3.md)）。
- ✅ **论文参考值订正**（commit `61ba882`）：对照 arXiv v4 Table 10 订正 7 个长预测数据集的 PAPER（旧值多处误抄）；Exchange/Solar/PEMS 原值正确。
- ✅ **长预测复现确认**：订正后 36 项偏差全部 <8%、其中 33 项 <3%（优秀线），无需调参。
- ✅ **PEMS pl48/96 发散修复并结案**（commit `5624d85`，复盘见 §7.3）：根因=训练无梯度裁剪 + 官方高 lr → epoch1→2 梯度爆炸；修复(grad_clip+lr减半)后发散全消除，PEMS 16 项 11✅/1🔶/4⚠️。残余 4 项（PEMS03/08 的 48/96）经增大 epoch（`EPOCHS=30`，best epoch 11 早停）+ 多 seed（`ITR=5`，5 seed 均值 0.258、方差 ±0.003）双重验证为配置客观极限，非 bug；刻意不为这部分偏离官方参数调参。
- 🎉 **复现工作全部完成**。后续若需：多 seed / epoch 工具已就绪（run.py seed=2023+ii、`run_PEMS.sh` 的 `EPOCHS`/`ITR` 环境变量）。

---

## 6. 待办计划

| # | 任务 | 负责方 | 备注 |
|---|---|---|---|
| 1 | ~~提供 GitHub 地址 → push~~ ✅ 已完成（origin=weiwant/iTransformer，`reproduce` 分支） | 开发机 | |
| 2 | ~~服务器搭环境 + 下载数据集~~ ✅ | 服务器 | conda `iTransformer` + pip cu118 |
| 3 | ~~冒烟测试~~ ✅ | 服务器 | `smoke_test.sh` 通过 |
| 4 | ~~**第一阶段**：严格默认参数跑全表~~ ✅ 已完成（52/52） | 服务器 | 结果见 §3 / [`part3.md`](part3.md) |
| ~~5~~ | ~~**第二阶段**：长预测调参~~ ✅ 已结案（参考值订正后偏差全 <8%，无需调参；见 §7.2 复盘） | 服务器 | commit `61ba882` 订正 PAPER |
| ~~6~~ | ~~**PEMS 48/96 发散问题 debug**~~ ✅ 已修复（commit `5624d85`，grad_clip+lr减半；发散全消除，11/16 达标） | 开发机+服务器 | 见 §7.3 / CLAUDE.md §8.10 |
| ~~6.5~~ | ~~**PEMS 剩余 4 项多 seed 优化**~~ ✅ 已验证无效（commit `57cc6c9`/`88e4d6a`；5 seed 均值 0.258、方差 ±0.003 = 配置极限），结案 | 服务器 | 见 §7.3 |
| 7 | 汇总：输出最终复现结论 | 开发机 | |

---

## 7. 第二阶段调参（已结案：参考值误抄，无需调参）

> 原计划对长预测偏差项调参；2026-06-30 复盘查明"偏差"源自论文参考值误抄（非欠拟合），订正后全部达标，**调参证伪、结案**。方向备忘见 7.1（历史），结案复盘见 7.2。

### 7.1 调参与分析候选方向（第一阶段全表跑完后讨论）

> 记录于 2026-06-25。基于训练流程代码分析（`exp_long_term_forecasting.py` / `utils/tools.py` / `run.py`），作为第二阶段调参和"为什么涨点"讨论的切入点。**等第一阶段全表结果出来后对照验证，再决定是否动手。**

**① 学习率调度优化**
- 现状：默认 `lradj=type1`，手写调度（`utils/tools.py:13-14`）：`lr * 0.5**(epoch-1)`，从第 2 个 epoch 起每个 epoch 减半；配合 `train_epochs=10`，到第 6-7 轮 lr 已塌到 ~1e-7 量级，基本停止学习。
- 问题：调度过于激进，长预测任务很可能在 lr 塌掉前还没学透——ETTh1 pred_len=336/720 最佳 epoch 出现在第 1 轮（§4.1）即强信号。
- 候选改法：换 `--lradj cosine`（需在 `adjust_learning_rate` 补 cosine 分支）、或把 type1 衰减放缓（如 `0.5**(epoch//3)`）。
- 待验证：换调度后 ETTh1 长预测 MSE 偏差能否从 +13.7% / +17.7% 降到 <5%。

**② 早停导致学习不充分（无波动容差）**
- 现状：`EarlyStopping`（`utils/tools.py:27-56`）`patience=3`、`delta=0`——要求 val_loss **严格下降**才算改进，无小幅波动容差。
- 问题：`delta=0` 对验证 loss 的正常抖动零容忍，易过早触发早停；§4.1 里 ETTh1 pred_len=336/720 只训 4 轮（最佳第 1 轮）就停，明显学不够。
- 候选改法：放宽 `patience`（如 5~10）、给 `delta` 小容差（如 1e-4）、或两者并用；与 ① 配合让训练真正多跑几轮、最佳 epoch 后移。
- 待验证：训练轮数能否增加、长预测 MSE 是否随之下降。

**③ 实例归一化 / RevIN（后续讨论"为什么是涨点关键"）**
- 现状：默认 `use_norm=True`（`run.py:86`），模型内做实例归一化（RevIN 式：每个样本独立 zero-mean/unit-var 归一化，预测后反归一化）。
- 待讨论（本阶段不动，仅记录）：为什么这一步对时序预测涨点如此关键——初步线索是它能消除不同序列/变量间的量纲与非平稳差异，让倒置后的"变量 token"处于同一尺度，与倒置 Transformer 结构协同发挥最大作用。等全表结果出齐（必要时配合消融），再展开讨论。

### 7.2 长预测"系统性偏差"复盘（已结案：参考值误抄，无需调参）

> 记录于 2026-06-30。原计划对长预测偏差项调参，执行后查明**偏差源自论文参考值误抄、非模型欠拟合**，第二阶段调参证伪、结案。

**起因**：第一阶段（`part3.md` 旧版）显示 ETTh1 336/720 偏差 +13.7%/+18.2%，疑似 `lradj=type1` + `train_epochs=10` 欠拟合，据此备好调参方案（cosine lr + epochs↑ + patience↑，commit `e73329a`）。

**转折（服务器执行 ETTh1 336/720 调参 `TuneC20p5`）**：结果与默认参数**完全一致**（MSE 小数点后都相同），且第 6 轮早停（patience=5）。排查后定位真因：
- 复现值 0.491/0.509 与论文 **Table 10 真实值 0.487/0.503** 仅差 +0.8%/+1.3%——本就在论文水平。
- `extract_results_full.py` 的 PAPER 把 ETTh1 336/720 误抄成 0.432/0.431（反常地比 192 还小），才造出 +13%/+18% 的**假偏差**。
- **调参为何无效**：best epoch 锁在第 1 轮（val_loss 第 1 轮即最低、之后不降反升），type1 与 cosine 第 1 轮 lr 几乎相同（1.0 vs 0.994 × base_lr），故两种配置加载的第 1 轮 checkpoint 几乎相同——恰恰证明模型早已收敛、无调参空间。

**订正**：对照 arXiv v4 Table 10（Appendix F.4）逐格订正 7 个长预测数据集的 PAPER（commit `61ba882`）；Exchange/Solar/PEMS 原值正确未动。重算后长预测 36 项偏差全部 <8%（33 项 <3%），原 5 个"待调项"全部达标。

**关于判定阈值**：维持 ✅<8% / 🔶8~13% / ⚠️>13%，另以 |偏差|<3% 为优秀线。合理基准是**论文自身种子波动**（论文 Table 5 用 5 种子平均报告）+ 硬件浮点差异（2060→4090、cuDNN），刻意不为 2~3% 的微差调参（避免过拟合到论文那一个种子）。

> ETTh1 调参中间产物（`TuneC20p5`）保留在 `result_long_term_forecast.txt` 作为"调参无效"的证据；`tune_*.sh` / `extract_results_tune.py` / `utils/tools.py` cosine 分支保留备用，不删。

### 7.3 PEMS pl48/96 发散修复复盘（2026-06-30，已结案）

> 第一阶段 PEMS pl48/96 出现 +37%~+919% 的灾难性偏差。本机复现→定位根因→修复→服务器验证→二次排查（epoch / 多 seed）→确认残余为配置极限，结案。

**① 现象**：PEMS pl48/96 全线偏高（PEMS03/96 +919%、PEMS07/48 +616%、PEMS08/96 +99%），pred_len 越大发散越狠；pl12/24 全达标（-3.8%~+7.4%）。

**② 根因定位（本机 RTX2060 完整复现）**：
- iTransformer 训练循环**无梯度裁剪**（`exp_long_term_forecasting.py`：`loss.backward(); model_optim.step()`）。
- 官方高 lr(0.001) + 长 pred_len（`projector=Linear(d_model, pred_len)` 输出维度大）→ **epoch1→2 单步梯度爆炸**，vali loss 翻倍飙升（PEMS08 pl96 实测 0.535→0.837）→ best 永远卡 epoch1。
- 排除：非硬件（本机 2060 复现）、非参数填错（与官方 4 个 PEMS 脚本逐字核对一致）、非评估口径（pl12/24 同口径达标）。

**③ 修复（commit `5624d85`）**：
1. `run.py` 加 `--grad_clip`（默认 0=关闭，**不影响长预测已复现结果**）。
2. `exp_long_term_forecasting.py` train 加 `clip_grad_norm_`（amp / 非 amp 两分支）。
3. `run_PEMS.sh` 对 pl48/96 加 `grad_clip=1.0` + lr 减半 0.001→0.0005（PEMS04 本就 0.0005，只加 clip）；pl12/24 原参不动。

**④ 本机消融验证（PEMS08 pl96，论文 0.221）**：

| 配置 | test MSE | 偏差 | 说明 |
|---|---|---|---|
| 官方原参（lr=0.001, 无 clip）| ~0.42 | +99% | 发散 |
| +grad_clip=1.0 | ~0.41 | +85% | clip 压住爆炸但 test 卡住 |
| +grad_clip + lr=0.0005 | 0.306 | +38% | vali 持续降到 epoch4，收敛 ✅ |
| +use_norm=1 | 0.601 | +172% | 恶化，否定（官方用 0 是对的）|

**⑤ 服务器结果（修复后，单 seed）—— 发散全部消除**：

| 子集·pl | 修复前 | 修复后 | 论文 | 判定 |
|---|---|---|---|---|
| PEMS07·48 / 96 | +616% / +698% | **-1.4% / +0.1%** | 0.110 / 0.139 | ✅ 优秀 |
| PEMS04·48 / 96 | +9.5% / +15% | +7.0% / +11.0% | 0.120 / 0.150 | ✅ / 🔶 |
| PEMS08·48 / 96 | +70% / +99% | +30.8% / +32.1% | 0.186 / 0.221 | ⚠️ |
| PEMS03·48 / 96 | +37% / +919% | +31.8% / +56.1% | 0.125 / 0.164 | ⚠️ |

PEMS 16 项总计：**11 ✅ / 1 🔶 / 4 ⚠️**（修复前 8 ✅ / 1 🔶 / 7 ⚠️ 且最差 +919%）。

**⑥ 剩余 4 项的二次排查（确认是配置极限，非 bug）**：
- **增大 epoch（`EPOCHS=30`）**：PEMS03 pl96 best epoch 11、epoch 14 早停，test 仍 0.256 → 单 seed 早已收敛，增大 epoch 无效。
- **多 seed（`ITR=5 EPOCHS=15`）**：PEMS03 pl96 五个 seed MSE = 0.256 / 0.261 / 0.258 / 0.256 / 0.261（均值 0.258, +57%，**方差仅 ±0.003**）→ 多 seed 无效，确认该配置（d_model=512, use_norm=1, lr=0.0005）的客观极限 ~+57%。

**⑦ 结案结论**：
- **硬成果**：PEMS 发散修复（grad_clip + lr 减半）将 pl48/96 从 +9.5%~+918% 降到 +7.0%~+57.1%，4 项达标/优秀、1 项接近、4 项残余。
- **残余偏差（PEMS03/08 的 pl48/96，+30~57%）= 当前配置的单 seed 客观极限**：增大 epoch 无效、多 seed 无效（方差 ±0.003）。论文 PEMS 报告值靠 5 seed 平均 + 可能有未公开训练细节，刻意不为这部分偏离官方参数调参（遵循 §7.2 判定基准）。
- **保留备用**：`--grad_clip`、`run.py` 多 seed（seed=2023+ii + cuda seed）、`run_PEMS.sh` 的 `EPOCHS` / `ITR` 环境变量。

---

## 8. 问题与解决

| 问题 | 现象 / 原因 | 解决 / 计划 |
|---|---|---|
| **CPU 版 torch** | `requirements.txt` 默认装 CPU 版，跑不了 GPU | 手动 `pip install torch==2.0.0 --index-url .../cu118` |
| **ETTh1 长预测"偏差>13%"（已查明）** | 旧论文参考值误抄（336/720 写成 0.432/0.431，真实 0.487/0.503）致假偏差；订正后 +0.8%/+1.3%，非欠拟合 | 已订正 PAPER（Table 10，commit `61ba882`）；调参实验证实 best epoch=1、无调参空间（§7.2） |
| **Traffic 训练中断** | pred_len=96 训练至 epoch 8 终端被关闭 | checkpoint 已存最佳模型，用 `--is_training 0` 加载续测得到结果 |
| **setting 目录名错位** | 目录名前缀与参数错位拼接 | 照训练脚本原样填参数；仅测试时参数须与训练逐字一致（详见 CLAUDE.md §8） |
| **torch 覆盖 / mkl 冲突** | 旧 `pip install -r requirements.txt` 含 `torch==2.0.0`，覆盖 GPU torch，`import torch` 报 `undefined symbol: iJIT_NotifyEvent` | 已从 requirements.txt 移除 torch 行；cu118 版单独装 |

---

## 9. 变更日志

- **2026-06-30（续2）**：PEMS 剩余 4 项（PEMS03/08 的 pl48/96, +30~57%）二次排查 + 全流程结案。① 增大 epoch（`EPOCHS=30`，依托 commit `88e4d6a` 的 env 变量）：PEMS03 pl96 best epoch 11、14 早停，test 0.256 未改善 → 单 seed 已收敛。② 多 seed（`ITR=5 EPOCHS=15`，run.py seed=2023+ii，commit `57cc6c9`）：PEMS03 pl96 五 seed MSE 0.256/0.261/0.258/0.256/0.261（均值 0.258, +57%, 方差 ±0.003）→ 多 seed 无效。确认残余为配置（d_model=512/lr=0.0005）客观极限，非 bug。PEMS 全流程结案：发散修复将 pl48/96 从 +9.5%~+918% 降到 +7%~+57%，16 项 11✅/1🔶/4⚠️。新增 §7.3 复盘，更新 §3.2/§4.2/§5/§6、CLAUDE.md §1/§8.10、part3.md。
- **2026-06-30（续）**：PEMS pl48/96 发散修复——本机复现并定位根因：iTransformer 训练循环**无梯度裁剪**，官方高 lr(0.001) + 长 pred_len(`projector=Linear(d_model,pred_len)` 输出维度大) → epoch1→2 单步梯度爆炸、vali loss 翻倍飙升、best 卡 epoch1（PEMS03/96 +919% / PEMS07 +698% / PEMS08 +99%，**非硬件**，本机 RTX2060 完整复现）。修复（commit `5624d85`）：`run.py` 加 `--grad_clip`（默认 0=关闭，不影响长预测已复现结果）、`exp_long_term_forecasting.py` train 加 `clip_grad_norm_`（amp/非amp 两分支）、`run_PEMS.sh` 对 pl48/96 加 `grad_clip=1.0` + lr 减半到 0.0005（pl12/24 原参不动）。服务器重跑后发散全部消除：PEMS07 pl48/96 达优秀(-1.4%/+0.1%)、PEMS04 pl48 达标(+7.0%)/pl96 接近(+11.0%)；PEMS03/08 的 pl48/96 仍 +30~56%（单 seed，论文 Table 5 用 5 seed 平均，待评估多 seed）。同步更新 §3.2/§4.2/§5/§6、CLAUDE.md §1/§8.10、`part3.md` §3.2/§4.10-4.13。
- **2026-06-30**：长预测复现确认 + 第二阶段调参结案——对照 arXiv v4 Table 10 订正 `extract_results_full.py` / `extract_results_tune.py` 的 PAPER（7 个长预测数据集旧值多处误抄，commit `61ba882`），删除被取代的 `extract_results.py`。重算后长预测 36 项偏差全部 <8%（33 项 <3%、达优秀线），原 5 个"待调项"全部达标。复盘 §7.2：服务器调参 ETTh1 336/720 结果与默认完全一致（best epoch=1、无调参空间），证实"偏差"源自参考值误抄而非欠拟合，**第二阶段调参证伪、结案**。下一步转 PEMS 48/96 发散 debug。同步更新 §3/§4/§5/§6/§8、CLAUDE.md。
- **2026-06-29（续）**：台账同步第一阶段完成状态——§3 进度总览改用 `part3.md` 的 9×4 + PEMS 4×4 总览表（100% 完成）；§4 由"逐项详细"改为"偏差分类 + 待调项清单"并指向 `part3.md` §4 看逐项（分层归档：主台账不冗长、序号自洽）；§5/§6 同步为"第一阶段完成、第二阶段进行中"、补 PEMS debug 待办。
- **2026-06-29**：第二阶段调参方案就绪——`utils/tools.py` 加 cosine lr 分支（开发机自测通过：lr 平滑递减）；新增 `tune_{ETTh1,ETTm2,Weather,ECL}.sh`（旋钮 env 覆盖、`--des` 隔离）+ `extract_results_tune.py`（按 des 分组对比）；§7.2 更新为方案就绪/待服务器执行。目标：长预测系统性偏差项 MSE 偏差 <5%。
- **2026-06-25（续）**：补齐 Table 1 其余数据集脚本——新增 `run_Exchange.sh`（汇率，长预测 8 变量，d_model=128）、`run_Solar.sh`（Solar-Energy，长预测 137 变量，`--data Solar`，solar_AL.txt）、`run_PEMS.sh`（PEMS **短预测** 12/24/48/96，4 子集 03/04/07/08，参数经 `pems_params` 查表）。同步更新 §1 目标（长预测 9 个 + 短预测 PEMS）、§3 进度表（拆分 3.1 长预测 / 3.2 PEMS）、CLAUDE.md §5/§7/§8、reproduce/README.md。注意：Exchange 官方脚本 pred_len=336 段的 `--train_epochs 1` 疑为笔误，本脚本改用默认 10；PEMS 数据集需另下载（`./dataset/PEMS/PEMS0X.npz`）。
- **2026-06-25**：在 §7 新增 7.1「调参与分析候选方向」——三点备忘：① 学习率调度优化（type1 减半过激进）；② 早停无波动容差致学习不充分；③ 实例归一化/RevIN（待后续讨论为何是涨点关键）。作为第一阶段全表跑完后的第二阶段调参与涨点分析切入点。
- **2026-06-24**：初始化本台账；迁移 0520 参照记录的 7 项已完成结果（ETTh1×4、Weather/ECL/Traffic×96，均严格默认参数）；建立项目文档框架（CLAUDE.md / 实验记录.md / .gitignore）。下一步：服务器 clone `reproduce` 分支 → 搭环境 → 跑全表。
- **2026-06-24（续）**：`origin` 改为 `git@github.com:weiwant/iTransformer.git`（SSH）；新建并推送 `reproduce` 分支（CLAUDE.md / 实验记录.md / .gitignore 已上传）。
- **2026-06-24（续2）**：新增 `scripts/reproduce/smoke_test.sh` 冒烟测试脚本（ETTh1 1 epoch 全流程 + `--is_training 0` 续测自检）+ `.gitattributes`（`*.sh` 强制 LF 行尾）。开发机自验通过后推送，待服务器冒烟。
- **2026-06-24（续3）**：修复 `requirements.txt`（移除 `torch==2.0.0`，避免覆盖 cu118 GPU torch 导致 `iJIT_NotifyEvent`）；CLAUDE.md 同步安装说明。服务器环境重建中。
- **2026-06-24（续4）**：永久化 LD_LIBRARY_PATH（新增 `scripts/reproduce/env_setup.sh`，smoke_test 与各 run_ 脚本开头 source）；新增 7 个按数据集训练脚本 `run_{ETTh1,ETTh2,ETTm1,ETTm2,Weather,ECL,Traffic}.sh` + `scripts/reproduce/README.md`。服务器可按数据集单独跑全表。
- **2026-06-24（续5）**：服务器环境就绪（pip cu118 torch + env_setup.sh 解决 libnvrtc）+ 冒烟测试通过；开始按数据集跑全 7×4 表。**新会话恢复：先读本文件 §3（进度表）/ §5（当前阶段）。**
- **2026-05-15**：（早期，RTX 2060）完成上述 7 项实验，记录于 `D:\wqWork\论文\开题报告\周报\0520\T4_iTransformer复现\`。
